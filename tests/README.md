# Test Layout

- `tests/unit` mirrors the Python package structure under `src/`.
- `tests/integration` holds cross-service and performance exercises.
- `tests/e2e` is reserved for user-focused end-to-end flows.
- `tests/fixtures` contains reusable data inputs.
