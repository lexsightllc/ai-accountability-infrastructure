# SPDX-License-Identifier: MPL-2.0
version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONDONTWRITEBYTECODE=1
    command: uvicorn ai_trust.api.main:app --host 0.0.0.0 --port 8000 --reload
